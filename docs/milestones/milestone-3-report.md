# Milestone 3 report: Firebase environment, native parity, and persistence spike

Status: **locally accepted**

Date: **2026-09-22**

Branch: `feature/milestone-3-firebase-spike`

Milestone base: `7d0bf7dfd2a6cb4d23d5d72bb76b20e4ebb55f47`

Acceptance-correction start: `079a1a5698639e91aae2bbe9915d2f3725555ee3`

Second corrective review start: `9e657fac52eb4384b1ea63f3f5bf93ce40e4b6b2`

Third corrective review start: `7951445a2586535dab6eb9cc01b4d0fb4497da74`

Fourth corrective review start: `12f268ac6bf69e81100389a1e0015a848d5df2d6`

Fifth corrective review start: `c1f79cbe62c8695ac21ffa84f179d387b515dfb5`

Sixth corrective review start: `83e33ff892c3e255e58829dbba1e213d1cb3f806`

Seventh corrective review start: `eed178f3f88f7c8f42a4fefaee718aa8a0f582e6`

## Outcome

Milestone 3 established an emulator-only Firebase backend, proved the selected booking, catalog-entitlement, assignment-publication and Rules primitives, exercised official Android and Apple Firebase adapters through native runtimes, and implemented encrypted UID-partitioned recovery persistence on both platforms. ADR-001 is accepted for official SDKs behind TillFailure contracts and the narrow Swift bridge.

This is local acceptance of the Firebase core/native-parity spike, not production approval. No Firebase cloud project or production resource was created or changed; nothing was deployed, merged, tagged or released; and no Milestone 4 behavior was added. Cloud ownership, production configuration, physical-device security, App Check, retention policy, release signing and deployment remain unresolved.

## Corrective review closure

First round (closed at `9e657fa`):

- Booking reschedule loads the stored appointment before replacement-bucket calculation and rejects trainer or client identity changes atomically. Tests prove the appointment/original locks remain unchanged, the alternate trainer receives no lock, and valid retries remain idempotent.
- Assigned-program publication puts the snapshot and every created `plannedWorkouts/{id}` path in a sorted, duplicate-free `requiredPaths` inventory. Zero/one/many-plan and failed-publication tests prove completeness and all-or-none commit behavior.
- Workspace restoration validates every computed entitlement count against zero and `maxMembershipsPerAccount`. Below-cap and exact-cap restores pass; above-cap restore leaves workspace, contributions, counts and authorization unchanged.

Second round (PR #3 review `pullrequestreview-5260762387`):

- Assignment replacement requires an explicitly supplied expected predecessor revision and reads the predecessor header and its workspace discovery index inside the publication transaction before any write. Missing predecessors, account/workspace/client/trainer identity mismatches, stale revisions, and terminal or already-replaced predecessors are rejected; receipt replay of a successful replacement still returns the original result without re-retiring anything. A deterministic `Promise.allSettled` concurrency test proves exactly one of two competing replacements of the same live predecessor commits, and the loser leaves no successor header, index, plan, manifest or receipt.
- A successful replacement retires both predecessor representations in the same commit: the account-owned header becomes `accessStatus=replaced`/`lifecycleState=terminal` and the workspace discovery index becomes `status=replaced`, each recording `replacedByAssignmentId`, `replacedAt` and the advanced revision. Trainer discovery queries then return only the successor. A missing, wrong-workspace, non-active, or revision-diverged predecessor index fails closed with no partial writes. The publication write budget counts both retirement writes (replacement adds 2, not 1) and the exact-budget test proves the ceiling.
- Membership activation enforces `maxMembershipsPerWorkspace` against the server-owned workspace `activeMembershipCount` while serialized through the expected workspace `membershipRevision`. Malformed, negative, and already-oversized counts, stale workspace revisions, and above-cap activations reject atomically with membership, workspace, entitlement, account and receipt state unchanged; below-cap and exactly-at-cap activations pass; replays never double-count. Suspension/restoration recomputes the same count, and a workspace filled to its cap remains atomically suspendable—the failure mode the finding described can no longer occur.
- Rescheduling writes every retained and newly acquired lock through the same lock writer initial booking uses, including the immutable `utcBucket` derived from the validated bucket calculation (never client-supplied) and the current `appointmentRevision`. Same-interval reschedules preserve every bucket identity; partially overlapping reschedules keep retained identities, give acquired locks the exact initial-booking field set, delete released locks, and replay/rejection leave lock documents byte-identical.

Third round (PR #3 review `pullrequestreview-5260982254`):

- **Callable authorization.** The Admin-SDK callable boundary no longer trusts authentication or request-supplied identifiers. `bookAppointment` reads the caller account, workspace, caller membership, and both participant accounts/memberships inside the booking transaction and requires an active account, active workspace, and active membership with a supported role; a trainer member may book only their own slots for an active client member, and a client member may book only themselves with an active workspace trainer. `rescheduleAppointment` and `cancelAppointment` enforce the same caller boundary, with cancellation additionally restricted to the appointment's stored participants. Authentication alone — including anonymous emulator identities — grants no booking authority; authoritative emulator fixtures are created only through isolated Admin-SDK test setup, never an authorization bypass. Authorization failures create no appointment, lock, or receipt, and every booking/reschedule/cancel receipt records the authorized `callerUid` so a different account can never replay or observe another caller's operation.
- **Plan reference integrity.** Publication builds the normalized set of snapshot workout IDs before the transaction and requires every `plans[*].workoutId` to reference exactly one of them. Absent, blank, or ambiguous references and blank/duplicated snapshot workout IDs reject the whole publication, so a ready assignment or complete-looking manifest can never contain a planned workout that cannot resolve its snapshot workout. The check is in-request only, bounded by the existing publication ceilings.
- **Guarded assignment closure.** `closeAssignment` is now a receipt-backed, revision-checked live-to-terminal transition: it requires the caller's expected header revision, reads the account-owned header and the workspace discovery index in the same transaction before any write, and accepts only an exact `ready`/`active` unexpired state with matching account/workspace/client/trainer/assignment identity and index revision. Previously replaced, archived, cancelled, revoked, expired, stale, or index-divergent assignments are rejected without incrementing revisions, rewriting terminal reasons or `replacedByAssignmentId`, changing timestamps, or creating a receipt; a different operation ID cannot close an already-terminal assignment, while the original caller-bound receipt replays a successful close.
- **Swift one-shot registry lifecycle.** `FirebaseNativeBridge` now pre-allocates the token and stores the cancellation entry exactly once before registering the SDK completion path (`beginOneShot`), and removes the entry from the registry when the completion settles — success or failure alike (`deliverOneShot`) — instead of retaining one closure per `getDocument`/one-shot `writeDocument`/`increment` until explicit cancellation or termination. A single `OneShotGate` serializes completion, cancellation, timeout, and termination so each callback is delivered at most once; a completion after cancellation is suppressed and a cancellation after completion is a safe no-op. Long-lived listener entries are still removed only on explicit disposal, and the harness proves registry non-growth, cleanup on success/failure/cancellation, suppression, terminate-time emptiness, and preserved epoch fencing through a Debug-only diagnostic.

Fourth round (PR #3 review `pullrequest-review-5261724751`):

- **Separate roster and contribution accounting.** The workspace document now carries two distinct server-owned counters instead of one overloaded count: `activeRosterCount` (active relationships) and `catalogContributionCount` (catalog contributions, always a subset of the roster). Activating an active membership increments the roster in an active **or** suspended workspace, so suspended activations can no longer accumulate an unbounded active roster that later blocks restoration; the roster cap is enforced identically in both workspace states. Suspension preserves the roster while zeroing contributions; restoration re-enables contributions only for valid active memberships from the bounded authoritative scan. The bounded scan is authoritative for workspace transitions: a stored roster or contribution count that disagrees with it, or an already oversized roster, fails closed (`workspace-roster-inconsistent`, `workspace-contribution-inconsistent`, `workspace-roster-bound-violated`) instead of silently transitioning. A zero-roster workspace can now suspend and restore with zero affected memberships (previously an empty roster produced an SDK error instead of a clean transition). Account entitlement counts remain contribution-based and distinct from the workspace roster.
- **Complete Firebase UID domain on both platforms.** iOS recovery validation no longer imposes an `[A-Za-z0-9_-]` whitelist that rejected valid Firebase identifiers such as `user@example.com` and surfaced them as `LOCKED`. Both platforms now validate through the shared `RecoveryUidContract` — non-blank, at most 128 UTF-16 code units (the Firebase Auth/Admin SDK bound, counted identically by Kotlin `String.length` and Swift `utf16.count`) — with no character whitelist, because only the SHA-256 digest names the partition file. Raw UIDs never appear in a path, traversal-like identifiers cannot escape the recovery root, distinct identifiers never normalize into one account, and the UID remains authenticated data so a partition copied under another UID still fails closed.
- **Canonical incrementable-value contract.** The Apple bridge's increment path previously recognized only `NSNumber`, so a counter stored as the string `"5"` (which the string-typed bridge itself writes) was treated as zero and rewritten as `1` while Android produced `6`. Both platforms now use the shared `CounterValueContract`: an integral stored value, or a canonical signed integer string (`-?[0-9]+`, in `Long` range) is accepted; missing or explicit-null fields start from zero; malformed strings, decimals, whitespace, plus-prefixed strings, booleans, floating point, collections, and other unsupported types are rejected with the stable non-retryable `INVALID_ARGUMENT` failure and never overwrite the stored value; and addition overflow is detected through the shared exact-addition helper. Transaction retry, cancellation, exactly-once delivery, registry cleanup, and epoch fencing are preserved.

Fifth round (PR #3 review `pullrequestreview-5276336981`):

- **Fail-closed entitlement source counts.** Before workspace suspension or restoration applies any membership contribution delta, the fan-out transaction now requires every affected entitlement's existing `activeMembershipCount` to be an integer in the full configured range `0..maxMembershipsPerAccount`; every computed count is independently checked for the same integer shape and bounds before writes. This matches the directly equivalent single-membership transition. Above-cap, negative, string, and fractional stored counts reject atomically; the regression for `21 -> 20` proves that workspace state/revision, membership contribution/revision, account, entitlement status/count/revision, command receipt, and Rules authorization all remain unchanged. Zero and maximum valid boundaries still restore/suspend correctly.
- Only the Node 22 TypeScript/backend/Rules scope changed and was rerun for this correction. The Android/iOS results below retain their fourth-round attribution; no native build, host, device, simulator, harness, or cold-launch result is claimed as rerun.

Sixth round (PR #3 review `pullrequestreview-5283411876`):

- **Revision-checked, receipt-backed account lifecycle.** `setAccountEnabled` was an unconditional update, so a delayed or retried enable could reactivate an account and its catalog entitlement after a newer disable had committed. It is replaced by `transitionAccountLifecycle`, which requires the caller, a nonempty idempotency key, the account UID, the requested state, an explicit `expectedLifecycleRevision`, and the configured cap; reads the receipt, account, and entitlement inside the transaction before any write; requires the stored lifecycle revision to be an integer equal to the expected value; and commits account status, entitlement status, the advanced revision, and an `account-lifecycle` receipt atomically. Stale enables and stale disables are rejected (`stale-revision`), identical caller-and-payload retries replay without advancing the revision or rewriting the receipt, a different caller or changed payload can never replay or observe another command, and two concurrent opposite commands on the same revision commit at most one winner. The same required-identity check and cap validation now also guard the membership and workspace commands.
- **Strict enable-time entitlement-count validation with security-first disable.** Enabling an account validates the stored `activeMembershipCount` as an actual integer within `0..maxMembershipsPerAccount` before changing either document, so `"1"`, `1.5`, `true`, `null`/missing, arrays, objects, negative, and above-cap values are rejected instead of being coerced by `count > 0` into an active account whose Rules still deny every catalog read; a valid `0` leaves the entitlement inactive. Disable always writes `inactive` — malformed or above-cap state can never hold catalog authorization open — and preserves the stored count verbatim, so malformed state blocks re-enable until a separate trusted reconciliation repairs it. One strict integer/bound helper is now shared by the membership, workspace, and account paths, and configured caps are validated as bounded integers (account cap `1..20`, the same ceiling the Rules entitlement check accepts; workspace roster cap `1..99`, derived from the documented write budget). Rules-integrated regressions prove that a rejected malformed or above-cap enable leaves account, entitlement, revision, timestamps, and receipts unchanged while catalog reads stay denied, and that a legitimate disable denies reads while a stale enable cannot restore them.
- **Participant identifiers accept the representable Firebase UID domain.** Booking-callable validation no longer bans embedded dots, so email-shaped participant UIDs such as `trainer@example.com` work instead of failing before authorization. Validation is now split by identifier type: participant UIDs accept the UID domain the raw path model can represent (non-blank, at most 128 UTF-16 code units, dots/punctuation/Unicode allowed, no path separator, no `.`/`..`, no reserved `__.*__` document-ID form, and no normalization), while workspace, appointment, and idempotency identifiers keep their stricter single-segment validation. A valid Firebase Auth UID containing `/` cannot be expressed by this Milestone 3 `users/{uid}` scheme and is rejected fail-closed with `invalid-argument` instead of being silently retargeted; complete identity handling remains Milestone 4 schema work. Emulator tests prove dotted booking, reschedule, and cancellation, single-segment lock naming, caller-bound receipt identity, forged email-shaped participant rejection, and unchanged authoritative authorization reads.
- Only the Node 22 TypeScript/backend/Rules scope changed and was rerun for this correction. No shared Kotlin, Android, iOS, Swift, dependency, or lockfile change was made; the Android/iOS results below retain their fourth-round attribution and no native rerun is claimed.

Seventh round (PR #3 review `pullrequestreview-5294122577`):

- **Fail-closed lifecycle schema validation.** The Firestore Rules authorize catalog access only for `schemaVersion == 1` account and entitlement documents, so a trusted command could previously enable an account (or publish an active entitlement) on a record whose schema version was missing, malformed, or unsupported and report success while every catalog read stayed denied. One strict schema helper is now applied to every path that reports access as active: account enable requires the current schema on `users/{uid}` and the fixed entitlement, and membership activation plus workspace restoration require it before writing `status: "active"`. Rejections (`account-schema-unsupported`, `entitlement-schema-unsupported`) leave account, entitlement, revision, timestamps, and receipts untouched, and nothing is silently migrated.
- **Deactivation always remains possible.** Disable, membership revocation, and workspace suspension deliberately skip the schema requirement, so a damaged or unknown version can always be deactivated: authorization becomes inactive, the stored count and (unlike a migration) the schema version are preserved verbatim, and the receipt is still written with the normal revision binding. Access cannot be granted again until a separate trusted process restores the schema, which is documented as Milestone 4 identity work rather than an implicit repair.
- **Apple recovery writes fail before committing, never after.** The Apple bridge configured the protection class and backup exclusion on the target *after* the atomic rename, so a metadata failure returned `LOCKED` even though the new payload had already replaced the committed record. Both attributes are now applied to the temporary file before the rename, any pre-commit failure removes the temporary file and leaves the previous record byte-identical, and the rename is the only commit point with no fallible work after it. Encryption, UID-bound associated data, Keychain key handling, fsync, atomic replacement, temporary cleanup, and locked-recovery behavior are unchanged.
- **Regression fixes in the harness itself.** The tamper injection could be a no-op when the ciphertext already began with the replacement character, which made `tamperFailsClosed`/`lockedDeletePreserves`/`lockedReplacementPreserves` intermittently fail on a legitimate record; the injection now flips to a guaranteed-different base64 character and asserts that the injected bytes actually changed.
- Only the backend TypeScript/tests and the Apple recovery write sequence changed. The signed Swift harness (recovery plus Firebase bridges at runtime) and the Xcode Debug build were rerun; shared Kotlin, Android, and the Firebase bridge contract are unchanged, so their results keep their earlier attribution.

Thirteenth round (PR #3 review `pullrequestreview-5308890124`, finding `4097371681`):

- **A terminated client no longer poisons the next one in the same process.** `FirebaseFirestore.getInstance(app)` (Android) and `Firestore.firestore(app:)` (Apple) keep returning the same Firestore instance for as long as its app object exists, so after `terminateAndClear()` a client constructed for the same project — the natural sign-out/sign-in path — could receive the *terminated* singleton and fail every operation with no recovery short of restarting the process. Both platforms now own their app instances through a per-project generation registry: a generation is closed synchronously when teardown starts, so a client constructed concurrently with or after teardown can never be handed the dying app or its Firestore/Auth singletons; the next client acquires a brand-new app with a unique name (`tillfailure-<project>-<n>`) and therefore fresh SDK instances regardless of when the retired app's cleanup finishes; and every operation on a retired client fails closed with a stable, non-retryable `FAILED_PRECONDITION` instead of silence or a result that could be mistaken for the new instance's.
- **Retired clients and their epochs stay fenced.** A client whose teardown ran refuses reads, writes, increments, pending-write waits, session observers and document listeners, so an old client can never touch a newer generation's instances, and an epoch retired by teardown is never accepted by a newer client.
- **Repeated teardown is deterministic.** A repeated `terminateAndClear()` on the same client reports success without touching the SDK again, and a partial-cleanup failure still retires the generation and is reported rather than hidden.
- **Platform-specific app deletion, with the reason recorded.** Apple deletes the retired app asynchronously (`FirebaseApp.delete(_:)`) *after* its Firestore instance is terminated and its persistence cleared. Android deliberately does **not** call `FirebaseApp.delete()`: deleting an app makes later by-name SDK lookups of components that still reference it fail with `IllegalStateException: FirebaseApp was deleted` — reproduced even inside a *new* client's `FirebaseAuth.useEmulator()` call — so the retired Android app stays as inert, unreferenced state while the closed generation is what guarantees a usable replacement. Emulator endpoint configuration is applied exactly once per generation, so two live clients for the same project can no longer raise `Cannot call useEmulator() after instance has already been initialized`.
- **Teardown completion survives a released client.** `FirebaseNativeBridge.terminateAndClear` captured itself weakly, so a caller that released its last reference while the SDK was finishing (an account session ending, a harness cycle completing) could wait forever for a teardown that had already finished; the teardown now holds a strong self-reference until it reports.
- **Same-process regressions were added on both platforms.** Android API 34 instrumentation and the signed Swift harness both create → sign in → write → server-read → terminate/clear → create again for the same project → operate, assert that the retired client's reads and writes fail with `FAILED_PRECONDITION` and deliver nothing, assert that retired-epoch callbacks never reach a recreated client, and repeat the cleanup/recreation cycle before a final live client operates.
- **Rerun scope.** The shared Firebase error-code contract, the Android adapter plus its device-test fixture, the new Android app-generation registry, and the Swift bridge/harness changed, so the Android host tests, the Android connected device tests, the iOS shared simulator tests, the signed Xcode Debug build and the signed Swift harness were all rerun. No backend TypeScript, Rules, Storage rule, UI, dependency, version catalog, lockfile, or generated asset changed, so the Node 22 backend/Rules suite (80/80) and the visual evidence keep their earlier attribution.

## Runtime and dependency evidence

| Area | Exact version/evidence |
|---|---|
| Node.js | `22.23.2`, isolated `npx` runtime; Functions emulator reported `Using node@22 from host` |
| npm | `11.12.1`; locked `npm ci` completed |
| Firebase CLI | `15.30.2`; clean local startup and shutdown |
| Firebase Functions/Admin/JS | `7.4.0` / `14.4.0` / `12.19.0` |
| Rules unit testing / TypeScript | `5.0.2` / `5.9.3` |
| Firebase Android BoM | `34.19.0`, API 34 emulator runtime |
| Firebase Apple SDK | SPM `12.19.2`, signed iPhone 16e/iOS 26.2 simulator runtime |

`npm ci` reported deprecation notices for transitive `node-domexception`, `json-ptr`, `uuid` and `glob`, plus eight moderate audit findings. No `npm audit fix`, dependency update or lockfile change was made. Xcode retained the existing ICU minimum-simulator and always-run script-phase warnings; the Storage emulator retained its Java `sun.misc.Unsafe` warning.

## Backend proof

Final Node 22 emulator run after the twelfth corrective round: **80 passed, 0 failed** on two consecutive clean runs (the earlier rounds' final runs were 17/17, 31/31, 43/43, 48/48, 50/50, 64/64, 67/67, 69/69, 71/71, 74/74, and 76/76).

| Primitive | Tests and result |
|---|---|
| Assigned publication | 16/16: atomic replay, deterministic complete zero/one/many-plan inventory, plan-to-snapshot reference integrity (absent/blank/duplicated/mixed rejection with no writes), revision-checked replacement retiring header and discovery index atomically with trainer-discovery exclusion, replacement replay without re-retirement, oversize all-or-none rejection, exact write budget counting both predecessor writes, stale/missing/malformed-request rejection with unchanged state, terminal and already-replaced predecessor rejection, predecessor/index identity and inconsistency rejection, single-winner competing-replacement concurrency, guarded live-to-terminal closure for each terminal reason, terminal/replacement-metadata immutability with receipt replay, and single-winner competing-close concurrency, non-single-segment path IDs (separators in user, workspace, assignment, predecessor, workout, exercise, and plan IDs, including the reported `day/exercises/item` workout ID) rejected before any reference or write with no header, snapshot, manifest, plan, index, or receipt, and punctuated UIDs and IDs still materializing at their exact paths |
| Booking | 13/13: bucket coverage, first-booking contention, idempotent reschedule/cancel, immutable trainer/client rejection, transaction-budget rejection, same-interval reschedule `utcBucket` preservation, retained/acquired lock schema parity with initial booking plus removed-bucket deletion, lock stability across replay and rejected reschedule, participant authorization rejection matrix (anonymous/disabled/suspended/missing/revoked/cross-workspace/wrong-role/unrelated participant/forged fields) with no partial state, reschedule and cancellation authorization boundary with revoked-membership and non-participant rejection, caller-bound receipt non-replay, fail-closed concurrent membership revocation, and dotted/email-shaped participant UIDs across booking, reschedule, and cancellation with single-segment lock names, caller-bound receipt identity, forged-participant rejection, and unchanged appointments on rejection |
| Callable input validation | 5/5: participant identifiers accepted across the representable UID domain (plain, email-shaped, dotted, punctuated, Unicode, padded, and maximum-length 128) with no normalization; empty/blank/overlength/separator/reserved-document-ID/non-string values rejected; non-UID path identifiers kept strict; malformed epoch and buffer values rejected |
| Callable | 4/4: unauthenticated and anonymous-without-authoritative-data rejection with no partial state, seeded trainer end-to-end authorization with idempotent replay and stored `callerUid`, different-caller receipt non-replay through the live Functions emulator, and forged-identifier/malformed-payload rejection at the boundary, including email-shaped participant UIDs reaching authorization (`permission-denied`) while separator injection in any identifier still fails validation before Firestore access |
| Catalog lifecycle | 28/28: count lifecycle without drift on replay/revoke/restore, workspace/account transitions, fan-out rejection, below/exact/above-cap restoration, pre-delta rejection of above-cap/negative/malformed entitlement counts during suspension with complete unchanged-state assertions, valid zero/maximum boundaries, roster-cap activation below/exactly-at/above cap with unchanged rejected state, roster-cap enforcement for suspended workspaces (including repeated attempts that cannot bypass the cap and a full-roster restoration afterwards), revoke in active and suspended workspaces without drift, roster and contribution counters kept distinct, fail-closed drift/oversized/malformed counters, zero-roster suspension and restoration, independent account-cap and workspace-cap rejection, stale workspace revision, at-cap suspension and restoration, revision-checked receipt-backed account enable/disable (valid active-to-disabled and disabled-to-active transitions, exactly-once lifecycle-revision advance, idempotent replay without receipt rewrite, caller and payload binding with cross-caller non-replay, stale enable after a newer disable, stale disable after a newer enable, missing/fractional/negative/string/boolean/lower/higher expected revisions, malformed stored revision, and exactly one winner for two concurrent opposite commands), strict enable-time count validation (zero/one/at-cap accepted with the Rules-visible status; above-cap, negative, numeric string, fractional, boolean, null, missing, array, and object rejected with unchanged state and no receipt), malformed/oversized configured-cap rejection, and security-first disable that always deactivates authorization while preserving malformed forensic counts and blocking re-enable, plus fail-closed lifecycle schema validation (missing, malformed, and unsupported `schemaVersion` on the account or its entitlement rejected for enable with unchanged documents and no receipt, the identical enable succeeding after a trusted repair, membership activation and workspace restoration refused because they would publish active access, and disable, revocation, and suspension still removing access without repairing the stored schema), plus per-membership identity/schema/role/contribution/lifecycle validation during the shared suspension/restoration scan (mismatched `userId`/`workspaceId`, missing and unsupported `schemaVersion`, invalid role, non-boolean contribution, and malformed revision each rejected atomically with unchanged workspace, membership, account, entitlement/authorization, and receipt state; a suspension-path rejection; a valid restoration still succeeding; and no cross-account entitlement contamination), plus pre-delta validation of a stored membership in the direct membership command (missing and non-boolean contribution flags, `userId`/`workspaceId`/schema/role/status/revision defects rejected atomically with unchanged workspace/account/entitlement counters, documents, and receipt, while a valid revoke/restore cycle still commits), plus delta-classified deactivation (revoking one of two contributions and suspending one workspace while another contribution remains both commit on a damaged account or entitlement schema with the stored status preserved, the schema unrepaired, and Rules-denied reads unchanged; additions, restorations, and re-activations on the same records still reject atomically with unchanged state and no receipt; a drifted stored `inactive` status is never activated by a removal; replay stays idempotent and a trusted repair restores normal transitions), plus zero-delta lifecycle changes (revoking an active relationship in a suspended workspace and re-suspending an already suspended workspace commit with a positive count contributed elsewhere while the drifted `inactive` entitlement stays `inactive`, replay is idempotent, and a genuine addition still re-derives `active`), plus workspace-schema and lifecycle gating for contribution increases (a missing or unsupported workspace schema and an unrecognized lifecycle state reject activation and restoration atomically with unchanged workspace, account, and entitlement state and no receipt; activation in a valid workspace still works; withdrawals, neutral changes, and suspension of a schema-damaged workspace still commit; and a trusted repair restores the previously rejected transition) |
| Firestore/Storage Rules | 14/14: catalog, above-cap suspension rejection preserving denied catalog authorization and all lifecycle state, malformed and above-cap enable rejection keeping catalog reads denied with unchanged state and no receipt, schema-rejected enable (missing account schema, malformed account schema, unsupported entitlement schema) keeping catalog reads denied with unchanged state and no receipt followed by a permitted disable and a revision-checked repair enable that restores reads, deny-after-disable with stale-enable refusal and revision-checked re-enable, assigned snapshot/source denial, stable forged-owner denial, server-authoritative lifecycle/count/lock/index/receipt write denial, Storage owner/MIME/metadata/size checks, and malformed-membership restoration rejection preserving denied catalog authorization with unchanged workspace/membership/entitlement/receipt state and no indirect catalog access until the stored identity matches the document path, and the documented own-account/workspace assignment-header collection query succeeding for the owner with multiple trusted headers (including a terminal one) while another account, a disabled account, a revoked membership, and a suspended workspace are denied, listing a terminal header granting no content, the trusted writer's header identity equalling its document path, and a strict direct `get` still denying forged and malformed headers, and catalog reads staying denied after a partial deactivation while the account or entitlement schema is damaged (a positive remaining count and an `active` status grant nothing until a separate trusted repair, and a mere removal never activates a drifted `inactive` entitlement; only a genuine addition restores the read), and catalog reads staying denied through a zero-delta revocation that preserves a drifted `inactive` entitlement with a positive count contributed elsewhere, with access returning only after a genuine contribution addition, catalog reads staying denied while a damaged workspace blocks the contribution increase (schema rejection with unchanged workspace and entitlement state and no receipt, then a trusted repair authorizing the read), and Storage ownership (owner uploads and owner delete succeed, oversized and metadata-less uploads and wrong-MIME uploads rejected, cross-account, foreign-path, and malformed-path deletes denied, MIME/size/metadata checks retained on create/update) |

The probes validate the primitives under configured bounds. They do not approve final product caps, booking-rights policy, or a deployed environment.

## Native parity matrix

| Case | Android actual | Apple actual | Attribution |
|---|---|---|---|
| Auth and accepted read/write | PASS | PASS | Android instrumentation; signed opt-in Swift harness |
| Rules rejection mapping | `PERMISSION_DENIED` PASS | `PERMISSION_DENIED` PASS | Client SDK paths only; no Admin bypass |
| Cache/pending metadata | Offline cache + pending true, then server + pending false PASS | Offline cache + pending true, then server + pending false PASS | Metadata-change listeners on both |
| Listener disposal | No callback after disposal PASS | No callback after disposal PASS | Native registrations removed |
| Stalled explicit cancellation | `CANCELLED`, exactly once; late SDK completion fenced PASS | `CANCELLED`, exactly once; late SDK completion fenced PASS | Networking disabled before valid local write |
| Independent stalled timeout | `DEADLINE_EXCEEDED`, exactly once PASS | `DEADLINE_EXCEEDED`, exactly once PASS | 300 ms configured harness timeout; deterministic latches/groups |
| Account-epoch fencing | Old-epoch completion/callback suppressed PASS | Old-epoch completion/callback suppressed PASS | Network re-enabled and current epoch drained |
| Terminate/clear and lifecycle | PASS | Real Settings background/foreground and terminate/clear PASS | Emulator/simulator runtime |
| Terminate, clear and same-process recreation | Terminate/clear, then the retired client denies reads and writes with the stable non-retryable `FAILED_PRECONDITION` and delivers no callback; a client created afterwards for the same project signs in, writes and server-reads on fresh SDK instances; a retired-epoch callback never reaches it; two further cleanup/recreation cycles and a final live client PASS | Same, including real Settings background/foreground before teardown, three recreated clients, retired-client read/write denial, retired-epoch fencing and repeated cycles PASS | Android API 34 instrumentation; signed opt-in Swift harness, thirteenth corrective round |
| One-shot registry lifecycle | Not rerun (Android bridge unchanged) | Success/failure/cancel cleanup, cancel-after-completion, suppressed late completion, at-most-once delivery, no growth across repeated reads/writes/increments, listener retention/disposal, terminate emptiness PASS | Signed opt-in Swift harness, third corrective round |
| Recovery UID domain | Complete Firebase UID domain round-trip, empty/blank/overlength rejection, digest-only traversal-safe partitions, distinct partitions, cross-UID isolation PASS | Same cases plus wrong-UID decryption refusal PASS | Android API 34 instrumentation and signed opt-in Swift harness, fourth corrective round |
| Recovery write commit ordering | Not rerun (Android unchanged; its app-private `noBackupFilesDir` temporary file plus atomic move performs no fallible metadata work after the move) | Injected pre-commit metadata failure returns `LOCKED`, preserves the committed bytes and payload byte-for-byte, never commits the new payload, removes temporary files, and a following write commits and survives a restart; tamper/locked checks now deterministic PASS | Signed opt-in Swift harness, seventh corrective round |
| Canonical counter contract | Numeric-string/integral/missing/negative increments, malformed/decimal/whitespace/boolean/floating/collection/overflow rejection with the stored value unchanged, exactly-once delivery PASS | Same cases plus typed boolean/float/collection fixtures PASS | Android API 34 instrumentation and signed opt-in Swift harness, fourth corrective round |
| Encrypted restart recovery | Android Keystore-backed AES-256-GCM PASS | Keychain key + CryptoKit AES-GCM PASS | Actual platform implementations, not common fakes |

The underlying pending-write SDK task is not claimed to be physically cancellable. TillFailure’s exactly-once gate suppresses late completion after cancel, timeout or epoch change.

## Recovery persistence

The common recovery record contract remains Firebase- and platform-type-free. Both implementations use an explicit version-1 encrypted envelope with key identifier `tillfailure.recovery.v1`, random nonce per write, UID/version/key-ID authenticated data, temporary-file flush and atomic rename. Both platforms validate partition identifiers through the shared `RecoveryUidContract` (non-blank, at most 128 UTF-16 code units, no character whitelist) and name partition files by the SHA-256 digest of the raw UID, so the complete Firebase UID domain — email-shaped, punctuated, Unicode, and maximum-length identifiers — round-trips identically on Android and Apple while raw UIDs never appear in a path. Existing unreadable data is authenticated before replacement or deletion; corruption, tamper, wrong/missing key and key loss produce a locked error rather than empty success. Apple configures the required protection class and backup exclusion on the temporary file before the atomic replacement, so a metadata failure aborts the write with the previous committed record intact and the temporary file removed, and the rename is the only commit point with no fallible work after it.

- Android stores a non-exportable AES-256 key in Android Keystore and files under app-private `noBackupFilesDir`. API 34 instrumentation proved round trip/recreation, UID isolation, absent plaintext, unique nonce, envelope version/key ID, no-backup location, atomic replacement, temporary cleanup, tamper/wrong/missing-key lockout and preservation of unresolved files.
- Apple stores a random AES-GCM key in Keychain with `AfterFirstUnlockThisDeviceOnly`, writes app-private Application Support files with backup exclusion, and requests `completeUntilFirstUserAuthentication` Data Protection. The signed Swift harness proved the same round-trip, isolation, envelope, nonce, tamper/key-loss and preservation cases. The simulator accepted the protection configuration but returned no observable protection-class attribute; physical-device protection semantics are therefore not claimed.

Future rotation uses a new versioned key identifier, reads old envelopes with their matching retained key, atomically rewrites each UID partition under the new key, and deletes the old key only after audited migration completion. Full production rotation is not implemented. Hardware backing, secure erase and physical-device Data Protection are not claimed.

## Verification ledger

| Gate | Final result |
|---|---|
| Node 22 TypeScript build and emulator start/stop | PASS (rerun for the twelfth corrective round under Node `22.23.2`; locked dependencies retained) |
| Trusted-operation and Rules suite | **80/80 PASS** (twelfth corrective round; two consecutive clean runs) |
| Common metadata + Android/iOS compilation | PASS (rerun for the thirteenth corrective round: shared metadata, Android compilation/debug assembly and the iOS Debug build all succeeded) |
| Android host tests | **63/63 PASS** (rerun for the thirteenth corrective round; the shared Firebase error-code contract changed) |
| Android connected tests | **67/67 PASS** on API 34 (rerun for the thirteenth corrective round: the previous 66 checks plus the same-process termination/recreation regression) |
| Android debug assembly/cold launch | Android debug assembly rerun for the thirteenth corrective round (`assembleDebug` PASS); Android cold launch retained from `9e657fa` |
| iOS shared simulator tests | **63/63 PASS** (rerun for the thirteenth corrective round) |
| Native signed Xcode Debug build | PASS (rerun for the thirteenth corrective round as an ad-hoc **codesigned** build: the opt-in harness needs a real code signature because a signing-disabled linker signature leaves the Keychain unavailable, which makes Auth fail with its keychain error and the recovery bridge report `LOCKED`) |
| Swift Firebase/recovery harness | PASS; **107 asserted checks (64 Firebase + 43 recovery), 0 failures** (rerun for the thirteenth corrective round), including the same-process terminate → recreate → operate cycles with retired-client read/write denial, retired-epoch fencing and repeated cleanup/recreation on Apple, plus the earlier encryption, UID, registry, cancellation, epoch-fencing, terminate/clear and pre-commit metadata-failure cases |
| Android/iOS cold launch | iOS Debug cold launch rerun with the seventh-round build: Foundation Home remained the default destination with no harness output without the opt-in flag; Android cold launch retained from `9e657fa` |
| Diff/secret/generated/machine-path audit | PASS at the acceptance commit and the second through thirteenth corrective commits |

The fourth corrective round changed `firebase/functions` TypeScript sources/tests, shared Kotlin contracts and their tests, the Android adapter/persistence validation, the Android device-test fixtures, the `iosApp` Swift bridge/harness sources, the shared build file (test-only Firestore dependency for typed fixtures), and documentation: no production dependency, version catalog, or lockfile change. The Android and Apple parity suites were rerun because shared Kotlin, Android adapter, and Swift bridge code changed; the Android cold launch and the Android/iOS visual evidence are preserved from earlier commits with their original attribution.

The sixth corrective round changed only `firebase/functions` TypeScript sources (`catalog.ts`, `index.ts`, and the new `input-validation.ts`), their emulator/Rules/unit regressions, and documentation. No shared Kotlin, Android, iOS, Swift, build configuration, dependency, version catalog, or lockfile change was made, so no native suite was rerun and the native matrix above keeps its fourth-round attribution.

The seventh corrective round changed `firebase/functions/src/catalog.ts` with its emulator and Rules regressions, the Apple recovery bridge (`RecoveryPersistenceBridge.swift`) with its Debug harness, and documentation. No shared Kotlin, Android, dependency, version catalog, or lockfile change was made; the signed Swift harness and the Xcode Debug build were rerun, while the shared-Kotlin simulator suite and all Android suites keep their fourth-round attribution.

The eighth corrective round changed only `firebase/functions/src/catalog.ts` (per-membership identity/schema/role/contribution/lifecycle validation inside the shared workspace suspension/restoration scan) with its emulator and Rules regressions, and documentation. No shared Kotlin, Android, Apple, dependency, version catalog, or lockfile change was made, so every Android, iOS, shared-Kotlin, signed Swift harness, and Xcode build result above is retained without rerun.

The ninth corrective round changed `firebase/functions/src/catalog.ts` (one shared stored-membership validator reused by the workspace scan and the direct membership command, applied before any delta is derived), `firebase/firestore.rules` (the assignment-header collection `list` is now path-bound so the documented owner query is authorizable, while per-document `get` keeps the full identity proof), their emulator and Rules regressions, and documentation. No shared Kotlin, Android, Apple, dependency, version catalog, or lockfile change was made, so every Android, iOS, shared-Kotlin, signed Swift harness, and Xcode build result above is retained without rerun.

The tenth corrective round changed `firebase/functions/src/catalog.ts` (membership contribution deltas are classified as additions or removals, so the account/entitlement schema is required only for additions and for access the stored entitlement did not already have, and a partial removal never elevates the stored status), its emulator and Rules regressions, and documentation; no Rules change was needed. No shared Kotlin, Android, Apple, dependency, version catalog, or lockfile change was made, so every Android, iOS, shared-Kotlin, signed Swift harness, and Xcode build result above is retained without rerun.

The eleventh corrective round changed `firebase/functions/src/catalog.ts` (the same classification now distinguishes a neutral zero-delta change from an addition, so only an addition may raise the stored entitlement status and a revocation from a suspended workspace can never activate a drifted `inactive` entitlement as a side effect), its emulator and Rules regressions, and documentation; no Rules change was needed. No shared Kotlin, Android, Apple, dependency, version catalog, or lockfile change was made, so every Android, iOS, shared-Kotlin, signed Swift harness, and Xcode build result above is retained without rerun.

The twelfth corrective round changed `firebase/functions/src/assigned-program.ts` (every path-derived ID in publication and closure is validated as exactly one Firestore document segment before any reference or transaction), `firebase/functions/src/catalog.ts` (contribution increases require the source workspace to be schema-current and in a recognized lifecycle state), `firebase/storage.rules` (delete authorization separated from create/update validation so an owner can remove its own object), their emulator, Rules, and Storage regressions, and documentation. No shared Kotlin, Android, Apple, dependency, version catalog, or lockfile change was made, so every Android, iOS, shared-Kotlin, signed Swift harness, and Xcode build result above is retained without rerun.

The thirteenth corrective round changed the shared Firebase error-code contract (the new `FAILED_PRECONDITION` code), the Android adapter plus the new Android app-generation registry and its device-test fixture, the Swift bridge (per-project app generations, terminated-client fencing, idempotent teardown, and a strong self-capture that guarantees the teardown completion is delivered) with its Debug harness, and documentation. Emulator-backed native code and shared Kotlin changed, so the Android host tests (63/63), the Android connected device tests (67/67 on API 34, including the new same-process termination/recreation regression), the iOS shared simulator tests (63/63), the signed Xcode Debug build and the signed Swift harness (107 asserted checks, 0 failures) were all rerun. No backend TypeScript, Rules, Storage rule, UI, dependency, version catalog, or lockfile changed, so the Node 22 backend/Rules suite (80/80) and the Android/iOS cold-launch visual evidence are retained from their earlier commits.

The fifth corrective round changed only the entitlement lifecycle TypeScript implementation/tests and documentation. It added no dependency, lockfile, native, UI, generated, production-endpoint, credential, deployment, or Milestone 4 change. The complete Node 22 backend/Rules suite was rerun; all native evidence is preserved from the fourth round with its original attribution.

## Remaining decisions and deferred work

- Native resumable Storage upload/process-death recovery remains deferred to the first approved media/upload milestone, currently Milestone 9 or 11. The safe pending-upload registry remains required; this is not a Milestone 3 acceptance blocker.
- Product/security still own offline eligibility duration, locked-recovery retention/export/discard, booking and fan-out production caps, privacy/retention policy and shared-device requirements.
- Platform/security still own Firebase project/environment ownership, region/data residency, billing/budgets, service accounts, App Check, deployment, production credentials and release signing.
- Physical-device Keystore/Keychain/Data Protection behavior and production restore drills remain release evidence, not simulator claims.

## Evidence

- [Android cold launch](evidence/android-m3-cold-launch.png)
- [iOS cold launch](evidence/ios-m3-cold-launch.png)
- Generated build/emulator logs remain ignored; exact commands and sanitized results are summarized above.
